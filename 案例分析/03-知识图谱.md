# 03 · 知识图谱 — 核心知识体系框架

> 目标：建立个人知识体系，理解优先，按主线归类。
> 不要零散记忆，要按"架构风格 / 质量属性 / 技术领域"三条主线建立结构。

---

## 🌳 系统架构师案例核心知识体系

```
系统架构师案例核心知识体系
├── 架构风格与模式
│   ├── 分层架构（表现/业务/数据层）
│   ├── 微服务架构（服务拆分、治理）
│   ├── 事件驱动架构（解耦、异步）
│   ├── 管道-过滤器架构（数据流处理）
│   └── 仓库风格 / 黑板系统（共享数据中心）
├── 质量属性与架构评估
│   ├── 性能（响应时间、吞吐量）
│   ├── 可用性（冗余、容错）
│   ├── 安全性（认证、授权、加密）
│   ├── 可修改性（模块化、松耦合）
│   ├── 互操作性（接口标准化）
│   ├── ATAM / SAAM 评估方法
│   ├── 效用树构建
│   └── 风险点 / 敏感点 / 权衡点
├── 数据设计与数据库技术
│   ├── 关系型数据库设计（规范化、反规范化、E-R图）
│   ├── NoSQL 选型（Redis、MongoDB…）
│   ├── 缓存策略（穿透 / 击穿 / 雪崩）
│   └── 分布式事务（2PC、TCC、最终一致性）
├── 分布式系统与集成
│   ├── 微服务治理（注册发现、熔断限流、配置中心）
│   ├── 消息中间件（解耦、削峰）
│   ├── ESB 企业服务总线
│   └── RESTful API 设计（网关、负载均衡）
├── 软件工程与系统设计
│   ├── 开发模型（敏捷、DevOps、V模型、螺旋）
│   ├── 需求工程（获取、分析、规格、验证）
│   ├── 设计模式（工厂、观察者、策略…）
│   └── 测试策略（测试左移、自动化）
└── 新兴技术与融合
    ├── AI 系统架构设计
    ├── 大模型部署策略
    ├── 云原生（容器、K8s、Service Mesh）
    └── IoT 架构设计（设备/网络/平台/应用层）
```

---

## 一、架构风格对比表

> 更完整版与实例见 👉 [06-架构基础与风格.md](./06-架构基础与风格.md)

| 架构风格 | 核心思想 | 优点 | 缺点 / 挑战 | 适用场景 |
|----------|----------|------|--------------|----------|
| 分层架构 | 上层仅调用相邻下层，关注点分离 | 结构清晰、易理解、可并行开发、复用好 | 多层调用性能瓶颈；易过度设计；不适高并发低延迟 | 企业应用、传统信息系统 |
| 微服务 | 按业务能力拆为自治服务，轻量 API 通信 | 独立部署伸缩、技术栈灵活、故障隔离强 | 分布式复杂、运维成本高、一致性挑战 | 大型复杂互联网应用、快速迭代 |
| 事件驱动 | 组件经事件（消息）异步通信 | 高响应性、松耦合、高吞吐、易扩展 | 测试难、一致性保障复杂、消息顺序/可靠性需额外处理 | 高并发异步处理、实时数据流 |
| 管道-过滤器 | 过滤器封装独立功能，数据经管道流动 | 模块复用强、可维护可扩展、具并发性 | 不适交互强应用；关系数据流需协调；额外 IO 开销 | 编译器、图像处理、ETL |
| 仓库/黑板 | 多知识源协同处理共享黑板数据 | 解决复杂非结构化问题、灵活可扩展 | 控制逻辑复杂、难测试、并发访问控制难 | AI（语音/图像）、决策支持 |

---

## 二、质量属性场景树（效用树）

```
质量属性（效用树）
├── 性能
│   └── 响应时间 → 99% 请求 500ms 内完成
│   └── 吞吐量 → 支持 2 万并发用户
├── 可用性
│   └── 容错 → 单节点故障不影响服务（99.99%）
├── 安全性
│   └── 认证授权 → 防越权访问
├── 可修改性
│   └── 模块化 → 变更影响范围可控
└── 互操作性
    └── 接口标准 → 跨系统数据交换
```

> 练习：看到案例描述的具体需求，能立刻对应到质量属性，并给出量化场景。

---

## 二之一、ATAM 三要素速记

| 要素 | 定义 | 判别特征 | 例子 |
|------|------|----------|------|
| **敏感点** | 影响**一个**质量属性的要素 | 改它，单属性明显变 | 缓存最大内存 → 性能 |
| **权衡点** | 影响**多个**且冲突的质量属性 | 改它，利弊同现 | 冗余服务 → 可用性↑但成本/一致性↓ |
| **风险点** | 可能导致问题的潜在隐患 | "若…则出事" | 两服务耦合过高 → 影响独立扩展 |

> 完整质量属性策略与实例见 👉 [06-架构基础与风格.md](./06-架构基础与风格.md)

---

## 三、记忆建议

1. **按主线归类**，不零散记。
2. **对比表 + 口诀 + 思维导图**巧记精确内容（如 ATAM 步骤、七大质量属性、五种架构风格）。
3. **理论代入项目**：思考"这个理论在那项目中怎么体现？换方案会怎样？"

---

📌 下一步：备考计划与清单见 [04-备考计划.md](./04-备考计划.md)
