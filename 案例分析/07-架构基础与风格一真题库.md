# 07 · 试题一真题库（历年真题 + 高分答案 + 备考策略）

> 本文档整理软考高级架构师**案例试题一（必做题，25 分）**的历年真题、高分答案与答题模板。
> 试题一核心围绕三大考点：**质量属性识别（12 分）+ 架构风格/评估（6~8 分）+ 敏感点/权衡点/效用树（5~7 分）**。

---

## 🎯 核心结论：掌握几道题就够？

> ⚠️ **重要修正（原版过于乐观）**：试题一至少有 **7 种命题形态**，原版本库只扎实覆盖了"质量属性识别 + 六要素 + 效用树正向 + 风格优缺点"约 4~5 类，**约占一半分数**。另一半分数在 **评估过程类**（效用树反向填空、风险点/非风险点识别、ATAM 步骤与角色）与 **风格对比论证类**（二选一 + 按指定维度填表、组件图填空）上，原版几乎是"裸奔"。下列补强题已补齐这些形态。

| 技能 | 分值 | 说明 |
|------|------|------|
| 质量属性识别 | 12 分 | 必考，逐条对应六大属性 |
| 架构风格 / 评估方法 | 6~8 分 | 画组件图 + 说明适用性 / ATAM |
| 质量属性场景六要素 / 敏感点权衡点 | 5~7 分 | 刺激源-刺激-环境-制品-响应-响应度量 / 效用树 |

**重点真题优先级（补强后）**

| 优先级 | 真题 | 掌握要点 |
|--------|------|----------|
| ⭐⭐⭐ | 2025.05 大模型训练平台 | 质量属性 12 项识别 + 解释器风格 |
| ⭐⭐⭐ | 2025.11 电动车充电系统 | 质量属性 + 六要素 + 效用树 |
| ⭐⭐⭐ | 2024.05 网络约车平台 | 质量属性 + 敏感点/权衡点 + 微服务 |
| ⭐⭐⭐ | 2017 下半年（新增） | **效用树反向填空 + 风险点/非风险点识别**（评估过程线必练） |
| ⭐⭐ | 2020.11（新增） | **管道-过滤器 vs 仓库 二选一 + 三维对比表** |
| ⭐⭐ | 2016 下半年（新增） | **四维度风格对比表填空** |
| ⭐⭐ | 黑板风格（新增自编） | **知识源/黑板数据结构/控制 三件套 + 为何不用仓库** |
| ⭐⭐ | ATAM 过程（新增） | **九步 + 三类角色 + 方法选择** |
| ⭐⭐ | 2026.05 智慧养老家居 | 物联网分层架构 + 质量属性分析 |
| ⭐⭐ | 2022.下 客户服务系统（改编） | 分层 vs 事件驱动、可用性‑性能权衡点 |
| ⭐⭐ | 2021 智慧水务系统（改编） | 仓库架构、性能‑一致性权衡、六要素 |
| ⭐ | 2023.11 医疗信息系统 | 基础质量属性识别练习 |
| ⭐ | 2024.11 电信（补全） | 管道-过滤器/Lambda + 组件图填空 |

**结论**：**质量属性识别线（约 12 分）**靠前 3 道可稳拿；**评估过程线 + 风格论证线（其余约 13 分）**必须靠新增的 2017下 / 2020-11 / 2016下 / 黑板 / ATAM 五道补强题补齐，否则是裸奔。

---

## 🔑 质量属性识别速记口诀

| 质量属性 | 关键词信号 |
|----------|------------|
| **性能** | 响应时间、吞吐量、延迟、并发、XX 秒内 |
| **可靠性** | 无故障时长、MTBF、失效率、连续运行（如 720 小时无故障） |
| **可用性** | 故障恢复、切换、MTTR、可用时间占比（几个 9） |
| **安全性** | 加密、认证、攻击、防御、隐私、审计、权限 |
| **可修改性** | 新增功能、修改、扩展、迁移、XX 天内完成、模块化 |
| **可测试性** | 测试、模拟、验证、日志、调试 |
| **易用性** | 界面、操作、用户友好、学习成本、无障碍 |

> 答题技巧：写出质量属性**名称**即得分，无需展开。拿不准时优先写六大属性中最匹配的一个。

---

## 真题一 · 2024 年 5 月（上半年）：网络约车调度服务平台

### 【题干】
某公司拟开发网络约车调度服务平台。需求与质量属性描述：
- (a) 参与者包括乘客、出租车司机和平台管理员三类
- (b) 具备安全防护措施，能对黑客攻击行为进行检测与防御
- (c) 正常负载下，必须在 0.5 秒内响应用户操作
- (d) 7×24 小时运行，硬件故障时能在 5 分钟内自动恢复
- (e) 2 个月内完成新增"拼车"功能的开发与部署
- (f) 界面方便老年用户使用，字体可放大、操作流程不超过 3 步
- (g) 支持在模拟 10 万并发下进行压力测试
- (h) 采用微服务架构，服务间通过 RESTful API 通信
- (i) 某地区用户量增长 300% 时，可通过水平扩展应对
- (j) 记录所有操作日志，支持审计追溯

### 【问题 1】（12 分）识别每条对应的质量属性
| 编号 | 质量属性 | 判断依据 |
|------|----------|----------|
| (a) | 非质量属性（功能/参与者） | 描述用户角色 |
| (b) | 安全性 | 抵御黑客攻击、检测防御 |
| (c) | 性能 | 响应时间 0.5 秒 |
| (d) | 可用性 | 故障恢复时间、持续运行 |
| (e) | 可修改性 | 规定时间内完成新功能开发 |
| (f) | 易用性 | 界面操作便捷、适合特殊用户 |
| (g) | 可测试性 | 模拟环境压力测试 |
| (h) | 可修改性（架构服务于可修改性） | 微服务解耦便于修改 |
| (i) | 可修改性（可扩展性 / 可伸缩性） | 用户量增长 300% 时通过水平扩展应对——考查"系统能够扩展部署/扩容"的能力，标准答案优先写**可修改性（可扩展性）**；仅当题干明确强调"扩展后性能仍满足指标"才写性能 |
| (j) | 安全性（可审计性维度） | 记录所有操作日志、支持审计追溯——"审计追溯"是**安全性的可审计性维度**典型关键词，标准答案写**安全性**，不要犹豫写可测试性 |

### 【问题 2】（6 分）敏感点 / 权衡点
- **敏感点**：
  - (c) 描述的是**性能质量属性场景**（响应时间 0.5 秒）。影响该场景的架构决策——如服务拆分粒度、服务调用链路长度、负载均衡策略——才是**性能的敏感点**（服务拆分过细、多次网络调用显著增加延迟）。
  - (d) 描述的是**可用性质量属性场景**（5 分钟恢复）。影响该场景的架构决策——如冗余部署、心跳检测、自动切换机制——才是**可用性的敏感点**。
- **权衡点**：
  - (h)+(i) 微服务架构 = 性能与可修改性的权衡点。提升可修改性（独立开发部署），但服务间通信增加延迟影响性能。
  - (b)+(c) 安全防护 vs 性能 = 权衡点。加密/认证增加响应时间，需安全等级与性能间取舍。

### 【问题 3】（7 分）微服务架构适用性
- **适用性**：含乘客端、司机端、调度引擎、支付、评价等独立业务域，符合"按业务能力拆分"。
- **优点**：独立部署支持快速迭代（满足 e）；热点服务（调度引擎）可独立水平扩展（满足 i）；技术栈灵活。
- **缺点**：服务间通信增加延迟（影响 c）；分布式复杂度高、运维成本大；数据一致性保障困难（需分布式事务/最终一致性）。

---

## 真题二 · 2025 年 5 月（上半年）：在线大模型训练平台

### 【题干】
开发在线大模型训练平台，支持 Python 代码编写、模型训练和部署。用户提交 Python 代码，系统解析并匹配计算机资源，用户无需关心底层硬件。李工认为适合采用**解释器风格架构**。
- (a) 错误时异常请求不影响用户正常工作，并通知管理员
- (b) 方便操作，提供快捷键设置
- (c) 界面适配用户屏幕尺寸比例
- (d) 提交训练任务应在一分钟内提供硬件和资源
- (e) 新增 GPU 型号时无需修改核心代码即可适配
- (f) 对用户提交的恶意代码进行检测和拦截
- (g) 可在模拟环境下进行全链路压力测试
- (h) 响应时间不超过 2 秒
- (i) 支持未来从 x86 迁移到 ARM 平台
- (j) 连续运行时间不低于 720 小时
- (k) 运维人员更换时无需修改系统代码
- (l) 操作界面提供新手引导

### 【问题 1】（12 分）质量属性
| 编号 | 质量属性 | 关键词 |
|------|----------|--------|
| (a) | 可用性 | 错误不影响运行、通知管理员 |
| (b) | 易用性 | 快捷键、方便操作 |
| (c) | 易用性 | 界面适配、屏幕尺寸 |
| (d) | 性能 | 一分钟内响应 |
| (e) | 可修改性 | 新增 GPU 无需改核心代码 |
| (f) | 安全性 | 恶意代码检测拦截 |
| (g) | 可测试性 | 模拟环境、压力测试 |
| (h) | 性能 | 响应时间 2 秒 |
| (i) | 可修改性（可移植性） | 平台迁移 |
| (j) | 可靠性 | 连续运行时间（720 小时无故障 = 规定时间内不失效 = 可靠性/MTBF，非可用性） |
| (k) | 可修改性 | 运维更换无需修改 |
| (l) | 易用性 | 新手引导 |

### 【问题 2】（6 分）解释器风格架构图填空
```
用户接口层（输入）→ 解释引擎（核心）→ 存储区（状态管理）→ 输出接口（执行层）
   Python代码编辑器      词法→语法→语义分析      代码仓库/状态      GPU/CPU资源调度
                         →代码优化→资源匹配        /资源分配表        →训练任务执行
```
填空要点：① 输入接口（用户提交的 Python 代码）② 解释引擎（解析/编译/调度）③ 存储区（保存中间状态和配置）④ 输出接口（解析结果映射到硬件资源执行）

> 📌 **解释器架构标准四组成部分（教材口径）**
> | 组成部分 | 作用 |
> |----------|------|
> | 输入接口 | 接收用户输入的规则、脚本、语言或配置 |
> | 解释引擎 | 完成语法分析、语义解释、规则匹配、执行控制 |
> | 存储区 | 保存解释过程中的状态、数据、配置、中间结果 |
> | 输出接口 | 输出执行结果，或驱动外部设备/模块执行 |
>
> 考试答题规范写法（可直接套）：本系统需解析用户提交的 Python 代码，并根据资源调度规则解释执行，符合解释器架构特征。解释引擎负责代码解析与资源匹配，存储区保存任务状态与资源配置，输出接口驱动底层 GPU/CPU 资源执行训练任务。

### 【问题 3】（7 分）为何适合解释器风格
1. **语言解析需求**：需对 Python 代码做词法/语法/语义解释，正是解释器核心能力。
2. **灵活性**：不同结构代码动态解析执行，无需预编译，满足可修改性。
3. **平台无关性**：解释引擎屏蔽底层硬件差异（GPU/CPU/x86/ARM），实现抽象层。
4. **可扩展性**：新增 GPU/框架只需在解释引擎加适配规则，不改核心架构。
5. **逐步执行与调试**：支持逐步执行，便于调试模型代码、查看中间结果。

---

## 真题三 · 2025 年 11 月（下半年）：电动车充电管理系统

### 【题干】
充电管理系统架构评审，质量属性场景：
- (a) 修改交互界面功能，评估后承诺 10 人/天内完成
- (b) 网络故障时自动切换备用网络并正常运行
- (c) 界面便于残疾人使用
- (d) 国家政策变化需针对性修改系统功能
- (e) 支持在模拟真实环境高峰期和恶劣天气下压力测试
- (f) 新增 API 接口开发需在 3 个工作日内完成
- (g) 登录时密码加密存储
- (h) 意外断电时自动恢复，连续运行不少于 150 小时
- (i) 查询请求响应不超过 0.3 秒
- (j) 网络失效后 3 分钟内发现并启用备用网络
- (k) 运维人员更换时无需开发人员修改代码

### 【问题 1】（7 分）质量属性
| 编号 | 属性 | 依据 |
|------|------|------|
| (a) | 可修改性 | 修改界面、人/天度量 |
| (b) | 可用性 | 故障自动切换 |
| (c) | 易用性 | 便于残疾人使用 |
| (d) | 可修改性 | 政策变化导致功能修改 |
| (e) | 可测试性 | 模拟环境、压力测试 |
| (f) | 可修改性 | 新增 API、工作日期限 |
| (g) | 安全性 | 密码加密 |
| (h) | 可用性 | 断电恢复、连续运行 |
| (i) | 性能 | 响应时间 0.3 秒 |
| (j) | 可用性 | 网络失效、切换时间 |
| (k) | 可修改性 | 运维更换无需修改 |

### 【问题 2】（6 分）六要素描述（示例选 (h)、(j)）
**场景 (h)**
| 要素 | 内容 |
|------|------|
| 刺激源 | 系统外部（意外断电/硬件故障） |
| 刺激 | 意外断电或系统故障发生 |
| 环境 | 正常运行状态下 |
| 制品 | 整个充电管理系统 |
| 响应 | 自动恢复运行，无需人工干预 |
| 响应度量 | 连续运行 ≥150 小时；恢复时间 ≤3 秒 |

**场景 (j)**
| 要素 | 内容 |
|------|------|
| 刺激源 | 系统外部（网络设备/链路） |
| 刺激 | 网络失效 |
| 环境 | 系统正常运行期间 |
| 制品 | 网络通信模块 |
| 响应 | 发现故障并自动启用备用网络 |
| 响应度量 | 3 分钟内完成切换，恢复关键服务 |

### 【问题 3】（12 分）效用树
```
                    效用(Utility)
        ┌───────┬───────┼───────┬────────┐
        ▼       ▼       ▼       ▼        ▼
     可用性   性能   可修改性  安全性  可测试性/易用性
      (H)    (M)     (M)     (H)      (L)
    ┌─┴─┐    (i)  ┌─┴─┐    (g)    (e)(c)(k)
   (b)(h)(j)        (a)(d)(f)
    H  H  H          M  M  M
```
注：H=高优先级，M=中，L=低。叶子节点用 (重要性, 难度) 表示，如 (H,H)。

---

## 真题四 · 2026 年 5 月（上半年）：智慧养老家居系统

### 【题干】
开发智慧养老家居系统。张工提出典型物联网系统应采用**分层架构**；李工提出边缘网关层采用**时序数据库**。
需求：传感器（温湿度、烟雾、红外、门磁）实时采集；本地初步处理与异常判断（跌倒、烟雾报警）；数据上传云端长期存储与大数据分析；家属 APP 远程查看；异常 10 秒内推送告警。

### 【问题 1】（7 分）物联网分层架构
| 层次 | 本系统功能 |
|------|------------|
| 感知层 | 温湿度/烟雾/红外/门磁传感器，采集环境与行为数据 |
| 网络层 | WiFi/ZigBee/蓝牙传至边缘网关，再经 4G/5G/宽带上传云端 |
| 平台层 | 边缘网关预处理与异常判断；云端存储、大数据分析、规则引擎 |
| 应用层 | 家属 APP、告警推送、健康报表、紧急呼叫联动 |

### 【问题 2】（8 分）为何边缘网关用时序数据库
传感器数据是典型时间序列数据（带时间戳、反映指标随时间变化趋势）。优势：
- **高写入性能**：针对高频写入优化，适合持续产生数据
- **时间范围查询高效**：支持"过去 24 小时温度变化"等窗口查询
- **数据压缩率高**：相邻点相似度高，压缩比可达 10:1 以上
- **内置降采样**：自动聚合（秒级→小时级），节省存储
- **边缘部署轻量**：适合资源有限的边缘网关

### 【问题 3】（10 分）质量属性角度的架构决策
| 质量属性 | 架构决策 | 分析 |
|----------|----------|------|
| 性能 | 边缘计算（本地预处理） | 异常判断本地完成，避免全量上云往返，确保 10 秒内告警 |
| 可用性 | 边缘网关离线自治 | 网络中断时本地仍可告警（如烟雾报警），不依赖云端 |
| 可修改性 | 分层架构 + 模块化 | 新增传感器/修改规则只改对应层 |
| 安全性 | 数据加密传输 + 访问控制 | 老人隐私数据加密，APP 需身份认证 |

---

## 真题五 · 2023 年 11 月（下半年）：医疗信息管理系统

### 【题干】
大型医院综合医疗信息管理系统，需求：
- (a) 高峰时段响应时间不超过 3 秒
- (b) 更新界面适应老中青操作习惯，2 周内完成
- (c) 密码遵循复杂性规则（大小写、数字、特殊字符）
- (d) 主数据库故障立即切换备用，≤5 分钟
- (e) 支持新增子系统无缝集成
- (f) 准确识别并提示患者药物过敏信息

### 【问题 1 答案】
| 编号 | 质量属性 |
|------|----------|
| (a) | 性能 |
| (b) | 可修改性 |
| (c) | 安全性 |
| (d) | 可用性 |
| (e) | 可修改性（可扩展性） |
| (f) | 安全性（或功能性） |

---

## 真题六 · 2022 年下半年（系统架构设计师 案例‑1）：客户服务系统事件驱动重构

> 📝 **题源说明**：本题为**改编/重构题**，用于练习"分层 vs 事件驱动"对比与权衡点写法，**只练思路，不抠标准答案措辞**。

### 【题干】
某企业计划升级现有客户服务系统，原系统采用分层架构，随着业务扩张，出现如下问题：
1. 每新增一类客服业务，都需要修改核心业务模块代码，改动范围大；
2. 某一个子模块异常时，经常造成整个客服系统停止服务；
3. 模块之间调用链路长，高峰期用户查询响应缓慢。

架构团队计划重构系统，采用事件驱动架构，引入消息队列完成模块之间通信，实现业务解耦。

### 【问题 1】（6 分）分层架构与事件驱动架构的区别
| 对比维度 | 分层架构 | 事件驱动架构 |
|----------|----------|--------------|
| 调用方式 | 显式调用，上层直接调用下层 | 隐式调用（发布‑订阅），组件通过事件通知 |
| 耦合度 | 组件直接依赖，耦合度高 | 发布者无需感知订阅者，解耦，耦合度低 |

### 【问题 2】（9 分）引入事件驱动架构的收益与代价
| 质量属性 | 收益 / 代价 | 说明 |
|----------|------------|------|
| 可修改性 | 收益 | 业务模块解耦，新增客服业务只需新增订阅者，不需修改发布方代码，改动范围缩小 |
| 可用性 | 收益 | 订阅模块故障不会反向影响发布方，故障隔离，单个业务异常不会导致整个客服系统瘫痪 |
| 性能 | 代价 | 消息队列增加异步转发开销；高峰期消息堆积时查询延迟增加，带来性能损耗 |

### 【问题 3】（10 分）消息队列作为权衡点
**权衡点**：一个架构决策，同时正向影响一项质量属性，负向影响另一项质量属性。

本项目引入消息队列：
- 正向收益（可用性）：实现模块解耦、故障隔离，避免局部故障扩散，可用性提升；
- 负面代价（性能）：消息存储与转发产生额外开销，高峰期消息堆积增大请求延迟，降低系统性能。

因此，引入消息队列属于**可用性‑性能之间的权衡点**。

---

## 真题七 · 2021 年（案例‑1，质量属性改进题）：智慧水务监控系统

> 📝 **题源说明**：本题为**改编题**，仓库架构与六要素练习价值高，**只练思路，不抠标准答案措辞**。

### 【题干】
某城市智慧水务监控系统，当前采用仓库架构，数据库作为中央共享数据存储区。各个监测终端客户端独立读取数据库中的配置数据，上报水务监测数据。运行一段时间后项目组发现如下问题：
1. 数据库压力过大，高峰期查询缓慢；
2. 数据库为单点，一旦故障，全部监测终端无法工作；
3. 各个客户端业务逻辑分散，后期维护修改成本很高。

### 【问题 1】（7 分）仓库架构特点与本系统问题
**仓库架构特点**：存在一个中央共享数据仓库，各客户端模块主动访问仓库读写数据；仓库被动存储，不主动调度客户端。

**本系统问题**：
1. 所有终端直接访问数据库，数据库压力过高，性能下降；
2. 数据库为唯一数据节点，存在单点故障风险，可用性差；
3. 业务逻辑分散在各个监测客户端，修改维护困难，可修改性差。

### 【问题 2】（8 分）引入缓存集群的收益与代价
| 质量属性 | 收益 / 代价 | 说明 |
|----------|------------|------|
| 性能 | 收益 | 热点监测数据存入缓存集群，终端优先读缓存，减少数据库访问，查询响应速度提升 |
| 一致性 | 代价 | 缓存与数据库主库数据可能出现不一致，需额外设计缓存更新与失效策略，增加系统复杂度 |

### 【问题 3】（10 分）完整的质量属性场景（性能）
在系统高峰期并发 300 台监测终端上报数据的运行环境下，水务监测终端对水务监控平台发起水质数据查询请求，监控平台的数据查询模块返回水质监测结果，查询平均响应时间小于 400ms。

> **六要素对照**：环境（高峰期并发 300 台终端）→ 刺激源（监测终端）→ 刺激（水质数据查询请求）→ 制品（数据查询模块）→ 响应（返回水质监测结果）→ 响应度量（平均响应时间 < 400ms）

---

## 补充题 · 2024 年 11 月（下半年）：电信分布式数据处理系统（补全版）

### 【题干】
某电信运营商需构建分布式数据处理系统，对海量通话详单、上网日志（结构化）与信令数据（非结构化）做实时分析与离线挖掘。需求：
- (a) 日均处理 10 亿条记录，端到端时延要求 < 5 秒
- (b) 单节点故障不影响整体服务，自动切换
- (c) 新增一种数据源接入，2 周内完成适配
- (d) 敏感用户数据加密存储
架构师提出两种候选：A（管道-过滤器，逐阶段处理流数据）、B（Lambda 架构，批处理层 + 速度层 + 服务层）。

### 【问题 1】（质量属性识别）
| 编号 | 质量属性 | 依据 |
|------|----------|------|
| (a) | 性能 | 处理时延 < 5 秒、高吞吐 |
| (b) | 可用性 | 故障自动切换 |
| (c) | 可修改性 | 新增数据源适配周期 |
| (d) | 安全性 | 敏感数据加密 |

### 【问题 2】（风格选择 + 组件图填空）
本系统"实时流处理 + 离线批处理并存"，**优先选 Lambda 架构**：速度层扛实时（管道-过滤器思想）、批处理层做全量校正、服务层合并结果。

**管道-过滤器组件图填空**：`数据源 → (1) 过滤器（清洗）→ (2) 过滤器（解析）→ (3) 过滤器（聚合）→ 数据汇（Sink）`
**答案**：(1) 输入过滤　(2) 处理过滤　(3) 输出过滤（任何合理阶段名均可，关键是"单向流、前过滤器输出即后过滤器输入"）。

---

## 真题八 · 2017 下半年（架构评估专项）：效用树反向填空 + 风险点识别

> 📌 形态还原自 2017 下半年案例一（ATAM 评估题）。**这是原版最大缺口之一**：既练"给图反向填空"，又练"风险点/非风险点定义与识别"（13 分大问）。

### 【题干】
某大型企业协同办公系统采用 ATAM 评估，得到以下质量属性场景描述：
- (a) 工作日 9:00 高峰期 1000 并发用户登录，响应时间 ≤ 2 秒
- (b) 新增一种文档格式支持，2 周内完成开发上线
- (c) 服务器宕机后 1 分钟内自动切换备用节点恢复服务
- (d) 采用 OAuth2.0 + JWT 认证，防止越权访问
- (e) 数据库主从切换时保证数据不丢失
- (f) 支持在测试环境模拟 5000 并发压测
- (g) 界面支持深色模式与字体放大，方便视障员工
- (h) 引入消息队列解耦各模块，但增加端到端延迟
- (i) 关键业务数据加密存储，满足等保三级
- (j) 运维交接无需修改核心代码

### 【问题 1】（12 分）效用树反向填空
图 1-1 为部分效用树，请在 (1)(2) 填质量属性名，从 (a)~(j) 为 (3)~(6) 叶子选最恰编号：

```
                效用 Utility
     ┌─────────┬─────────┬─────────┬─────────┐
    (1)      性能      (2)      安全性
     H         M         H         M
   ┌─┴─┐              ┌─┴─┐    ┌─┴─┐
  (3) (c)            (4)(i)  (5)(d)
   H   H              M  H    M  H
                  ┌─┴─┐
                 (6)(f)
                  L  L
```

**答案**：(1) 可用性　(2) 可修改性　(3) (a)　(4) (b)　(5) (d)/(i)　(6) (f)/(j)

> 📌 技巧：先看树的分支位置——(1) 与性能并列且挂 (c) 故障恢复 → 可用性；(2) 与安全性并列且挂 (b) 新增功能 → 可修改性；叶子编号要回题干挑"最贴切"的那条。

### 【问题 2】（13 分）风险点 / 敏感点 / 权衡点定义与识别

**定义（必背，300 字内）**：
- **敏感点**：某个架构决策对某一质量属性有显著影响的物理点或参数（如缓存命中率之于性能）。
- **权衡点**：影响多个质量属性且效果相互冲突的架构决策（一升一降）。
- **风险点**：存在问题的架构决策所带来的潜在隐患，可能导致质量目标无法满足。
- **非风险点**：某架构决策对某项质量属性**不会带来负面影响**（注意：**≠ 优点**，只是"无负面影响"的判断）。

**从题干选最恰当一条**：
- 敏感点 → **(a)**：高峰期 1000 并发响应 ≤ 2s，服务拆分粒度/缓存策略等决策显著影响其性能。
- 权衡点 → **(h)**：消息队列解耦提升可修改性/可用性，却增加端到端延迟牺牲性能。
- 风险点 → **(e)**：主从切换"保证不丢数据"若同步机制设计不当，是潜在数据丢失隐患。

---

## 真题九 · 2020 年 11 月：管道-过滤器 vs 仓库 二选一 + 三维对比表

> 📌 形态还原自 2020.11 案例一。**注意**：本题不是"分析某风格优缺点"，而是 **① 先选一个并论证 ② 再按指定维度填对比表**。

### 【题干】
某在线软件开发系统需对源代码做词法分析 → 语法检查 → 度量计算 → 报告生成。架构师提出两方案：A 管道-过滤器、B 仓库（共享数据）风格。

### 【问题 1】（13 分）应采用哪种风格？并填表 1-1
**选择：管道-过滤器**。理由：各处理阶段是单向数据流、阶段间无共享状态、可独立替换过滤器，完全契合管道-过滤器；仓库风格适合多工具围绕共享数据反复读写的场景，本系统不需要。

| 比较维度 | 管道-过滤器 | 仓库风格 |
|----------|------------|----------|
| 数据处理方式 | (1) 数据以流在过滤器间单向传递，前过滤器输出即后过滤器输入 | (2) 各构件围绕中央共享数据仓库读写，靠数据变化触发 |
| 系统拓展方式 | (3) 新增/替换过滤器即可扩展处理阶段，构件相互独立 | (4) 新增知识源（客户端）读写同一仓库，依赖仓库结构 |
| 处理性能 | 支持并行与批处理、吞吐高；但逐阶段传递有数据复制开销 | 中央仓库易成瓶颈与单点，并发读写需加锁 |

---

## 真题十 · 2016 下半年：质量属性填表 + 四维度风格对比

> 📌 形态还原自 2016 下半年案例一。**按指定四维度（交互方式 / 数据结构 / 控制结构 / 扩展方法）填表**是高频扣分点。

### 【题干】
某系统拟从仓库风格重构为事件驱动风格。

### 【问题 1】（质量属性识别，简表）
同前模板，识别 (a)~(f) 对应属性即可（重点练"评估过程/论证"形态，此处从略）。

### 【问题 2】（7 分）两风格四维度对比，填 (1)~(4)
| 维度 | 仓库风格 | 事件驱动风格 |
|------|----------|--------------|
| 交互方式 | (1) 构件主动读写中央仓库（共享数据显式交互） | (2) 构件经事件总线发布/订阅，隐式调用 |
| 数据结构 | (3) 中央共享数据仓储，统一数据模型 | (4) 事件消息封装数据，在事件体中传递 |
| 控制结构 | 仓库/中央调度控制数据流向 | 事件触发，控制分散在订阅者 |
| 扩展方法 | 增加新知识源读写仓库 | 增加新事件订阅者，发布方无感知 |

---

## 真题十一 · 自编：黑板风格三件套填空（为什么不用仓库）

> 📌 黑板三要素（知识源 / 黑板数据结构 / 控制）是教材明确定义的高频填空点，原库未练，自补一道。

### 【题干】
某语音识别系统需融合声学模型、语言模型、发音词典等多源知识逐步推理出识别结果。

### 【问题 1】该系统适合哪种风格？填三空
**答案（黑板风格三要素）**：
- (1) **知识源**：各推理模块（声学评分、语言模型打分、发音匹配），独立且可被调度
- (2) **黑板数据结构**：中央共享的"问题状态空间"，存放逐步生成的部分解
- (3) **控制**：调度器，按策略选择"当前最适用的知识源"作用于黑板

### 【问题 2】为什么不用仓库风格？
仓库风格中仓库**被动存储**、各客户端主动读写且无统一求解控制；黑板强调由**控制部件主动调度知识源**、围绕同一问题空间迭代求解，适合"非确定性、需多源知识协同推理"的场景。若用仓库，缺乏统一调度，难以保证求解路径最优。

---

## 真题十二 · ATAM 评估过程（步骤 / 角色 / 方法选择）

> 📌 原库 ATAM 只作"识别三概念"一笔带过。**本库补强：ATAM 九步、四阶段、三类角色、为何选 ATAM**——理论详见 06-B 的 3.2.1（效用树见 3.3.1、4+1 视图见 3.6.2.1）。

### 【问题 1】（步骤默写）
ATAM 标准九步：① 表述 ATAM 方法 → ② 表述商业动机 → ③ 表述架构 → ④ 对架构方法分类 → ⑤ 生成质量属性效用树 → ⑥ 分析架构方法 → ⑦ 头脑风暴并确定场景优先级 → ⑧ 再次分析架构方法 → ⑨ 陈述结果。

### 【问题 2】（参与角色）
三类角色缺一不可：① **评估小组**（主持、记录四概念）② **项目决策者**（提供商业动机、拍板优先级）③ **架构涉众**（开发/测试/运维/用户代表，提供各自场景）。

### 【问题 3】（方法选择）
- 重点是**多质量属性冲突 + 架构决策敏感性** → ATAM
- 架构早期、**主要分析场景对架构影响** → SAAM（靠"直接/间接场景"分类）
- 需对多个改进方案做**成本效益排序** → CBAM

---

## 🧩 答题"三步法"

1. **第一问（质量属性识别）**：逐条读题干 → 找关键词 → 对应六大属性 → 直接写名称。
2. **第二问（架构风格/评估）**：
   - 考架构风格：画组件图 + 说明数据流 + 解释适用性（结合题干需求）。
   - 考 ATAM：识别敏感点、权衡点、风险点。
3. **第三问（分析/对比）**：分点作答，每点"**观点 + 理由 + 结合题干**"。

---

## 📌 备考建议

- **精练真题**：前 3 道（网约车 / 大模型平台 / 充电系统）为重中之重，反复做 2 遍以上。
- **形成套路**：把"质量属性识别"练成条件反射（见关键词即写属性名）。
- **掌握一种架构风格深度分析**：微服务 / 解释器 / 分层 任选，能画组件图、说适用性、列优缺点。
- **熟记六要素与效用树**：这是高频高分问，模板固定，背熟即可套用。
- **机考练习**：在电脑上完整写答案，熟悉输入与绘图。

---

📌 关联阅读：[06-架构基础与风格-新手向拆解.md](./06-架构基础与风格-新手向拆解.md)（风格/质量属性/敏感点理论基础；**ATAM 九步见 3.2.1、场景三分类见 3.3.1、4+1 视图见 3.6.2.1、风险点/非风险点见四概念节**）· [02-答题方法论.md](./02-答题方法论.md)（评分与表达）

---

# 📝 第一题答题模板库（考试直接套用）

## 模板 1：质量属性识别模板
**题目问**：请指出表中编号分别对应哪种质量属性。
**答题格式**（只要求名称就写名称，要求理由就按"属性 + 依据"）：
> (a) 性能 —— 关键词"响应时间 0.5 秒"
> (b) 可用性 —— 关键词"故障恢复 5 分钟内"

## 模板 2：架构风格优缺点模板
**题目问**：分析采用某架构风格的收益与代价。
> 该方案采用【事件驱动架构】。
> 从可修改性看：订单服务发布事件通知库存/积分/短信模块，新增下游模块不需修改订单服务代码，有利于快速新增业务功能。
> 从可用性看：模块通过消息中间件异步解耦，单个订阅模块故障不直接影响订单主流程，有利于提升可用性。
> 但事件驱动带来异步延迟、消息顺序、重复消费、事务一致性问题，也增加调试和测试难度。
> 因此该方案是在可修改性、可用性与性能、可测试性之间权衡。

## 模板 3：敏感点答题模板
> 敏感点是指某个架构决策对某一个质量属性有显著影响。
> 本系统中，【缓存策略】是性能的敏感点。原因是：缓存命中率显著影响订单查询响应时间；提高命中率可减少数据库访问、缩短响应时间，设计不合理则性能下降。

## 模板 4：权衡点答题模板
> 权衡点是指某个架构决策同时影响多个质量属性，且影响相互冲突。
> 本系统中，引入【消息中间件】是可用性与性能之间的权衡点。一方面实现异步解耦、故障隔离，提升可用性；另一方面消息存储转发带来额外延迟，高峰期可能堆积，降低性能。因此需权衡。

## 模板 4.5：风险点与非风险点答题模板（必背，对应真题八问题2）
> **风险点**：存在问题的架构决策所带来的潜在隐患（可能导致某质量属性目标无法满足）。本系统中，【数据库主从切换】是风险点——若同步机制设计不当，切换可能丢失数据。
> **非风险点**：某架构决策对某项质量属性**不会带来负面影响**（注意：**≠ 优点**，只是"无负面影响"的判断）。本系统中，【采用标准 REST 接口】对安全性并非风险点——它不削弱系统安全，但也不等于"更安全"。
>
> 💡 **易错**：非风险点常被打错成"优点"。答题时只判断"该决策是否给此属性带来负面影响"，无则写非风险点，切勿顺手写"提升了 XX"。

## 模板 5：质量属性场景六要素模板
> 在【高峰期并发 10 万用户观看直播】的环境下，【外部用户】对【播放服务】发起【视频播放请求】，系统需要【返回视频流】，且【平均首帧延迟不超过 500ms】。
> 对应六要素：刺激源=外部用户；刺激=播放请求；环境=高峰并发10万；制品=播放服务/CDN；响应=返回视频流；响应度量=首帧≤500ms。


# 🚀 新增预测练习题 · AI / 云原生新场景（2026 新大纲兜底）

## 预测题 A · 私有知识库 RAG 系统（新大纲高频预测题）
### 【题干】
某企业搭建私有知识库问答系统，流程：加载 PDF 文档 → 文本切块 → 生成向量嵌入 → 存入向量数据库；用户提问后查询向量库召回相关片段，拼接上下文送入大模型生成答案。

**问题 1（6 分）：该系统主体采用哪一种架构风格？**
> 参考答案：**管道‑过滤器（数据流风格）**。每个处理环节作为独立过滤器（切块、Embedding、向量检索、Prompt 组装、生成），文档 / 向量数据沿管道依次流转，阶段职责单一、可插拔。

**问题 2（7 分）：项目组调整向量检索 Top‑K 参数，增加召回文档片段数量。该决策属于权衡点，请说明理由。**
> 参考答案：权衡点是影响多个质量属性且效果冲突的架构决策。增大 Top‑K，召回知识片段变多，问答准确性提升；但是检索数据量变多，增加查询时延，性能下降。因此是**准确性‑性能之间的权衡点**。

---

## 预测题 B · 反向拆解训练题（给场景拆六要素，真题最爱这么考）
> 正向：文字 → 场景（已在 6.10.2 训练）；反向：场景描述 → 拆出 6 要素。考场上常直接给一段场景让考生补全六要素表。

### 【题干场景】
在"双十一"大促高峰期（并发 50 万用户下单）的运行环境下，外部用户通过 APP 向订单服务发起下单请求；订单服务在收到请求后调用库存校验与支付接口完成下单，要求 99.9% 的下单请求在 1 秒内返回结果。

**请拆分出该质量属性场景的 6 个要素：**
| 要素 | 你的作答 | 参考答案 |
|------|----------|----------|
| 刺激源 |  | 外部用户（APP 端） |
| 刺激 |  | 发起下单请求 |
| 环境 |  | 双十一大促高峰期、并发 50 万用户 |
| 制品 |  | 订单服务 / 订单处理模块 |
| 响应 |  | 调用库存校验与支付接口完成下单 |
| 响应度量 |  | 99.9% 请求在 1 秒内返回 |

> ⚠️ 反向拆解易错点：**制品**最容易漏写或写成"整个系统"——应写具体受影响的架构元素（订单服务）；**响应度量**必须写具体数值（1 秒 / 99.9%），只写"快速返回"不完整。

### 【题干场景 2】
正常网络环境下，网络设备故障导致某区域通信中断；系统在 3 分钟内检测到故障并自动切换至备用网络，保障关键业务不中断。

| 要素 | 参考答案 |
|------|----------|
| 刺激源 | 网络设备 / 链路（外部） |
| 刺激 | 网络失效 |
| 环境 | 系统正常运行期间 |
| 制品 | 网络通信模块 |
| 响应 | 发现故障并自动启用备用网络 |
| 响应度量 | 3 分钟内完成切换 |

---

# 🎯 刷题执行路线（补强后）

1. **第一梯队（二刷，质量属性线 12 分）**：网约车、2025.05 大模型、2025.11 充电；每道脱离答案手写三遍，严格用六要素写场景。
2. **第二梯队（新补，优先于原二梯，评估/论证线）**：2017 下（效用树反向 + 风险点）、2020-11（风格二选一 + 三维表）、2016 下（四维度对比表）、黑板三件套、ATAM 过程；每道先遮答案默写，再对照。
3. **第三梯队（只练思路不抠措辞）**：智慧养老、客户服务系统（改编）、智慧水务（改编）、电信（补全）。
4. **兜底**：RAG 预测题 A + 反向拆解训练题 B，应对新题材与反向考法。
5. **基础识别 5 道小题**（见 06-B 文档）快速自测，做到架构风格一眼识别。

### 考前一周"临门一脚"（锁死分数，不再背新东西）
- **死磕场景六要素完整表述（必拿 6 分）**：拿 07 真题里的 (h)/(j) 场景，遮住答案在电脑上敲一遍，必查"制品"和"响应度量"是否写具体数值（如"3 分钟内""≤500ms"）。
- **默写万能权衡句（必拿 5 分）**：把 6.11.3 万能句抄在草稿纸左上角（方案A能够提升XX…但是会带来XX代价…需要在架构设计时进行权衡），考场上先丢上去。
- **聚焦微服务 & 事件驱动利弊（必拿 10 分）**：背熟 6.6 对照表中这两行的收益/代价——微服务=可修改性↑但性能↓调试难；事件驱动=解耦↑可用性↑但异步延迟↓可测试性↓。
- **每日只看三处**：6.9 避雷清单 + 6.9.4 致命禁句黑名单（防低级失分）、6.10 六要素模板（场景题不丢分）、07 真题库"权衡结论"（培养语感）。


