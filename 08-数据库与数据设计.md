# 08 · 数据库与数据设计（选做题专章）

> 本模块是案例分析的**常考选做题**之一，也是综合知识（选择题）和论文的高频板块。
> 新考纲后不仅考传统关系型数据库，还大幅增加了 **NoSQL、NewSQL、大数据及云原生数据库**。
> 核心思想：理解**一致性、可用性、性能**之间的权衡（Trade-off）。

---

## 一、核心考点全景图

| 模块 | 高频考点 | 考查形式 | 重要度 |
|------|----------|----------|--------|
| 数据库设计 | E-R 模型、规范化（1NF~BCNF）、反规范化 | 案例必考 / 选择 | ⭐⭐⭐⭐⭐ |
| 关系代数与 SQL | 关系运算、复杂 SQL、存储过程/触发器 | 选择 / 案例 | ⭐⭐⭐⭐ |
| 事务与并发控制 | ACID、隔离级别、锁、MVCC | 选择 / 案例 | ⭐⭐⭐⭐⭐ |
| NoSQL/NewSQL | Redis/MongoDB/HBase 选型、CAP/BASE、分布式事务 | 案例 / 论文 | ⭐⭐⭐⭐⭐ |
| 数据库架构 | 读写分离、分库分表、主从复制、MPP | 案例 / 论文 | ⭐⭐⭐⭐⭐ |
| 数据安全与运维 | 加密脱敏、备份恢复、性能调优 | 选择 / 论文 | ⭐⭐⭐ |

---

## 二、各模块详细掌握内容

### 1. 数据库设计方法论（案例第一梯队）

**E-R 模型设计**
- 实体、属性、联系的识别与绘制
- 1:1、1:N、M:N 联系转换为关系模式的规则
- 弱实体、超类/子类（继承）的表示

**规范化理论**
- 函数依赖（完全 / 部分 / 传递）判断
- 1NF → 2NF → 3NF → BCNF 的判定与分解
- 无损连接性、保持函数依赖的验证

**反规范化技术（高频！）**
- 方法：增加冗余列、增加派生列、表合并/分割
- 适用：以空间换时间，提升查询性能
- 代价：数据冗余、更新异常、需额外维护策略

> 💡 **标准答题套路**（案例常给未规范化表或业务描述）：
> ① 画 E-R 图 → ② 指出当前范式级别 → ③ 进行规范化分解 → ④ 说明为何在某场景需反规范化

**范式 vs 反规范化（权衡）**
```
数据库设计决策
├── 查询性能要求极高? ──是──> 反范式化（增加冗余）
│                             优势: 减少JOIN, 提升查询性能
│                             劣势: 数据冗余, 更新异常, 维护成本高
└── 否 ──> 严格遵循范式（减少冗余）
           优势: 一致性好, 冗余低, 更新简单
           劣势: 查询可能多表JOIN, 性能下降
         ⚖️ 权衡: 空间换时间
```

### 2. 事务与并发控制（选择+案例双考）

**ACID 特性**：原子性、一致性、隔离性、持久性及其实现机制

**四种隔离级别**（解决脏读/不可重复读/幻读）
| 隔离级别 | 脏读 | 不可重复读 | 幻读 | 典型场景 |
|----------|------|------------|------|----------|
| 读未提交 | ✗ | ✗ | ✗ | 极少用 |
| 读已提交 | ✓ | ✗ | ✗ | 多数默认 |
| 可重复读 | ✓ | ✓ | ✗（InnoDB 防） | MySQL 默认 |
| 串行化 | ✓ | ✓ | ✓ | 强一致，性能低 |

**并发控制技术**
- 悲观锁 vs 乐观锁
- 两阶段锁协议（2PL）
- MVCC 多版本并发控制（MySQL InnoDB 重点）

**分布式事务**
- 2PC、3PC、TCC、Saga 模式
- 本地消息表、事务消息
- CAP 定理与 BASE 理论的权衡

### 3. NoSQL 与新型数据库（新考纲重点）

| 类型 | 代表产品 | 核心考点 | 适用场景 |
|------|----------|----------|----------|
| 键值型 | Redis | 数据结构、持久化(RDB/AOF)、集群、缓存策略 | 缓存、会话、排行榜 |
| 文档型 | MongoDB | BSON、索引、分片、聚合管道 | 半结构化、内容管理 |
| 列族型 | HBase/Cassandra | RowKey 设计、Region 分裂、LSM-Tree | 海量时序、日志 |
| 图数据库 | Neo4j | 节点/边/属性、Cypher、遍历 | 社交网络、知识图谱 |
| NewSQL | TiDB/OceanBase | Raft/Paxos、水平扩展、强一致 | 金融级 OLTP |
| 时序数据库 | InfluxDB/TDengine | 时间戳索引、降采样、保留策略 | IoT、监控 |
| 向量数据库 | Milvus/Faiss | HNSW/IVF 索引、相似度搜索 | AI/RAG、语义检索 |

> ⚠️ **新考纲新增**：向量数据库、云原生数据库（PolarDB、Aurora）、HTAP 混合负载。务必关注。

### 4. 数据库架构设计（论文+案例高频素材）

**读写分离**：主从延迟、强制走主库、中间件选型
**分库分表**
- 垂直拆分 vs 水平拆分
- 分片键选择（均匀性、查询亲和性）
- 全局 ID（Snowflake、Leaf、UUID）
- 跨库 JOIN / 分页 / 排序解决方案

**缓存架构**
- 模式：Cache Aside / Read Through / Write Through / Write Behind
- 三大问题防护（见第三节）
- 一致性方案：延迟双删、Binlog 订阅（Canal）

**高可用架构**
- 主从复制 + 自动故障转移（MHA/Orchestrator）
- 多活架构（同城双活、异地多活）数据同步

### 5. 数据安全与治理
- 加密：传输(TLS)、存储(TDE)、字段级加密
- 脱敏：静态 vs 动态
- 访问控制：RBAC、ABAC、行级安全
- 审计合规：SQL 审计、GDPR/等保
- 备份恢复：全量/增量/差异、RPO/RTO、灾备演练

---

## 三、缓存设计：提升性能的利器

```
分布式系统设计
├── 一致性要求极高?（金融核心账户）──是──> CP: 强一致（2PC）
└── 否 ──> 追求高可用与性能?
           ├── 是 ──> AP: 最终一致（缓存、MQ、最终一致性方案）
           └── 否 ──> CA: 单体系统（分区时无法保证）
        ⚖️ 权衡: 一致性 vs 可用性
```

### Cache-Aside Pattern（旁路缓存，最常用）
- **读**：先读缓存，命中返回；未命中读 DB，写入缓存
- **写**：先更新 DB，**再删除缓存**（而非更新缓存）

### 缓存三大问题与对策
| 问题 | 现象 | 核心解决方案 |
|------|------|--------------|
| **穿透** | 查不存在的数据，缓存和 DB 都没有，请求直击 DB | 布隆过滤器（快速判存在）、缓存空对象（短过期） |
| **击穿** | 极热点 Key 过期，大量并发同时穿缓存查 DB | 互斥锁（单线程查 DB）、逻辑过期（后台异步更新） |
| **雪崩** | 大量 Key 同时失效 / 缓存宕机，请求涌向 DB | 过期时间加随机值、高可用集群（Sentinel/Cluster）、熔断降级 |

### CAP vs BASE
- **CAP**：分布式系统不可能同时满足 C、A、P 三项中的两项
  - CP（强一致，如传统数据库）、AP（高可用+最终一致，如 DNS/多数 NoSQL）
- **BASE**：对 AP 的补充，强调基本可用 + 最终一致，允许软状态（中间态不影响可用性）

---

## 四、实战案例与真题解析（药品销售系统）

### 案例背景
某医药销售企业建线上药品系统，采用 MySQL + Redis 混合架构。规范化后表结构：
- 供应商(供应商ID，供应商名称，联系方式，供应商地址)
- 药品(药品ID，药品名称，药品型号，药品价格，供应商ID)
- 药品库存(药品ID，当前库存数量)
- 订单(订单号码，药品ID，供应商ID，药品数量，订单金额)

### 问题 1（9 分）：反规范化方法 + 本案例采用哪种？
**常见反规范化方法**：
1. **增加冗余列**：多表存相同列，避免 JOIN（本案例将"供应商名称""当前库存数量"冗余进"药品"表即此法）
2. **增加派生列**：存可计算列（如订单表存"订单总金额"而非每次计算）
3. **合并表**：频繁同查的表合并为宽表
4. **重新分组表**：垂直拆分为不同列组

**本案例方法**：增加冗余列。把来自"供应商"的"供应商名称"和"药品库存"的"当前库存数量"直接存入"药品"表，避免关联查询，提升性能。

### 问题 2（9 分）：解决反规范化的数据不一致（三种方法 + 本系统选哪种）
**三种方法**：
1. **实时触发器**：源表变更时触发器同步冗余列，实时性好但影响写入性能
2. **批处理脚本**：定期重算同步，实现简单但非实时
3. **应用层代码逻辑**：更新时显式同步，灵活可控但增加业务复杂度

**本系统应选**：应用层代码逻辑。原因：MySQL+Redis 混合架构下，应用层可在更新 MySQL 同时同步 Redis 缓存；相比触发器不影响写入性能、易与缓存协同；相比批处理能准实时一致，符合药品销售实时性要求。

### 问题 3（7 分）：Redis 类型选择 + 一致性
**类型选择**：
- ① 热销药品排名 → **ZSet**（有序集合，按销量 score 排序，天然支持排行榜）
- ② 药品详情缓存 → **Hash**（对象字段映射，内存效率高）或 String（JSON 序列化）
- ③ 购物车 → **Hash**（用户ID 为键，商品ID 为 field，数量为 value）

**一致性方案**：
- 强一致（不推荐）：读写锁/分布式事务，性能损耗大
- **最终一致（推荐）**：Cache-Aside 模式（先更 DB，删缓存）
- 补充：延时双删（更新 DB 后删缓存，延时几秒再删一次，留主从同步时间）；或订阅 Binlog（Canal）异步更新/删除缓存

> 💡 **案例启示**：经典题综合考查反规范化（空间换时间）、一致性保障（应用层同步）、缓存类型选择（ZSet/Hash）、更新策略（Cache-Aside）。**权衡**是核心思想。

---

## 五、不同科目应对策略

| 科目 | 考查特点 | 备考重点 |
|------|----------|----------|
| 综合知识 | 覆盖面广、概念辨析 | 刷真题，重点范式、事务、NoSQL 分类、SQL 语法 |
| 案例分析 | E-R 图 + 规范化 + 架构选型 | 精练 5~8 道数据设计真题，掌握模板 |
| 论文 | 结合项目论述 | 准备 2~3 个数据设计素材（分库分表、缓存、迁移） |

---

## 六、推荐学习路径

```
第1周：规范化理论 + E-R模型（做题巩固）
  ↓
第2周：事务并发 + SQL进阶（MVCC、隔离级别）
  ↓
第3周：NoSQL/NewSQL + 分布式数据库（对比记忆）
  ↓
第4周：架构设计专题（分库分表、缓存、高可用）
  ↓
持续：刷近5年真题数据设计题 + 整理论文素材
```

---

## 七、避坑提醒

1. **不要只学 MySQL**：新考纲要求掌握多种数据库，TiDB、Redis、MongoDB、向量库都可能出题。
2. **不要忽视"为什么"**：架构师考试不是 DBA 考试，重点在**选型依据、权衡分析、架构决策**，答题体现 Trade-off。
3. **反规范化比规范化更重要**：实际架构常为性能主动违反范式，是案例和论文加分点。
4. **关注国产数据库**：达梦、OceanBase、TiDB、GaussDB 比重上升，了解特性与场景。
5. **答题要全面**：问"解决方案"不仅要给名称，最好简述原理与适用场景。

---

📌 关联阅读：[06-架构基础与风格.md](./06-架构基础与风格.md)（质量属性/权衡）· [07-试题一真题库.md](./07-试题一真题库.md)（答题模板）· [02-答题方法论.md](./02-答题方法论.md)
